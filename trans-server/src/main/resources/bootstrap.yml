spring:
  application:
    # 配置文件的前缀名称
    name: microservice-foo
  cloud:
    config:
      discovery:
        enabled: true
      uri: http://localhost:10087/
      profile: dev
      label: master
